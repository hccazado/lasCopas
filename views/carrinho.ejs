<%-include('./partials/header')%>

    <!--<div class="container">
        <h3 class="text-muted mt-4">Carrinho</h3>
        <% for(produto of produtos){ %>   
        <div class="row mt-3" style="--bs-gap: .25rem">
            <div class="col g-col-4 item-img">
                <img class="rotulo" src="<%=produto.rotulo%>" alt="rotulo">
            </div>
            <div class="col g-col-6">
                <span><%=produto.finca%><br></span>
                <%=produto.uvas %>
            </div>
            <div class="col g-col-4">
                Total = <br>
                <%=produto.qtd%> X R$ <%=(produto.valor).toFixed(2)%>
                <br>
                R$<span><%=(produto.qtd*produto.valor).toFixed(2)%></span>
                <%produto.total=produto.qtd*produto.valor%>
            </div>
            <% } %>
        </div>
    </div> -->
    <div class="container">
        <h3 class="subtitulo mt-4">Carrinho</h3>
        <div class="table-responsive">
            <table class="table align-middle table-striped mt-3">
                <thead>
                    <tr>
                        <th scope="col">Rotulo</th>
                        <th scope="col">Finca - Uvas</th>
                        <th scope="col">Valor</th>
                    </tr>
                </thead>
                <tbody>
                    <%for(produto of produtos){%>
                        <tr class="border-bottom">
                            <td><a href="/produtos/<%=produto.id_produto%>"><img src="<%=produto.rotulo%>" class="rotulo_tabela" alt="rotulo"></a></td>
                            <td><a href="/produtos/<%=produto.id_produto%>"><span><%=produto.finca%></span><br><%=produto.uvas%></a></td>
                            <td>Total: <br><%=produto.qtd%> X R$ <%=(produto.valor).toFixed(2)%>
                                <br>
                                R$<span><%=(produto.qtd*produto.valor).toFixed(2)%></span>
                                
                        </tr>
                    <%}%>
                    <tfoot>
                        <td></td>
                        <td>Total da Compra:</td>
                        <% var total = 0;
                        for (produto of produtos){
                            total += produto.qtd*produto.valor;
                        } %>
                        <td>R$ <%=total.toFixed(2)%></td>
                    </tfoot>
                </tbody>
            </table>
        </div>
    

        <div class="container-fluid d-flex mt-5">
            <div class="col">
                <form class="form-floating">
                    <input type="text" class="form-control" id="cep" placeholder="CEP de envio">
                    <label for="cep">CEP para envio</label>
                </form>
            </div>
            <div class="col">
                <% var total = 0;
                        for (produto of produtos){
                            total += produto.qtd*produto.valor;
                        } %>
                        Total: <span class="text-danger font-weight-bold">R$ <%=total.toFixed(2)%></span><br>
                        Frete: O frete é gratis :)
            </div>
            <div class="col">
                <a class="btn btn-success" href="/checkout/pagar">Comprar!</a>
                
            </div>
        </div>
    </div>

<%-include('./partials/footer')%>